## 변경 사항

- 정확도가 나쁜 표본만 받은 세션에서 표본 압축 후 위치 권한이 거부되면 `idle`로 초기화되던 문제를 수정했습니다.
- 메모리 압축과 세션 저장 모두 유효 기준점이 없으면 마지막 표본을 `segmentStart: true`로 보존합니다. 권한 거부 시 `paused`로 전환되어 활동 시간과 종료 기능이 유지됩니다.
- 압축 후 권한 거부, 정확도가 나쁜 첫 표본을 저장·복원한 후 권한 거부에 대한 회귀 테스트 2개를 추가했습니다. 일시정지 이후 시간이 활동 시간에 포함되지 않는지도 검증합니다.

## 확인

- [x] 재현 테스트 추가
- [x] `git diff --check` 통과
- [ ] 테스트 실행 및 확인

## 기타사항

`frontend`에서 `npm test -- src/features/map/useCourseTracking.test.ts` 실행을 시도했으나 실행 환경에서 npm을 찾지 못해 테스트가 시작되지 않았습니다. Node.js 설치 위치 조회를 위한 권한 요청이 거절되어 추가 실행을 진행하지 못했습니다. 기존 테스트와 신규 테스트의 통과 여부는 아직 확인하지 못했습니다.
